# ── Runtime ───────────────────────────────────────────────────────────────────

# Web framework & server
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0

# File upload support (needed by POST /api/v1/ai/train/file)
python-multipart>=0.0.9

# Trainable intent model (TF-IDF + Logistic Regression)
scikit-learn>=1.4.0
joblib>=1.3.0
numpy>=1.26.0

# MySQL knowledge-base storage (optional; embedded fallback if not set up)
pymysql>=1.1.0

# ── Dev / tests only (not required to run the server) ─────────────────────────
# httpx  # needed by fastapi.testclient (used for quick inline API checks)
